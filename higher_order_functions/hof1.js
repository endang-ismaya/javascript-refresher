let numbers = [10, 19, 20, 30, 45];

// forEach
numbers.forEach((element) => {
    console.log(element);
});

// map
let doubleNumbers = numbers.map((number) => number * 2);
console.log(doubleNumbers);
